import { Router } from 'express';import { createChallan,getChallan,listChallans,updateChallanStatus } from '../controllers/challanController.js';import { allowRoles } from '../middleware/auth.js';import { asyncHandler } from '../utils/http.js';
const router=Router();router.use(allowRoles('Admin','Sales','Warehouse','Accounts'));router.get('/',asyncHandler(listChallans));router.get('/:id',asyncHandler(getChallan));router.post('/',allowRoles('Admin','Sales'),asyncHandler(createChallan));router.patch('/:id/status',allowRoles('Admin','Sales'),asyncHandler(updateChallanStatus));export default router;
