import {X,Inbox,ChevronLeft,ChevronRight,LoaderCircle} from 'lucide-react';
export function Badge({children,tone}){return <span className={`badge ${tone||String(children).toLowerCase()}`}><i/>{children}</span>}
export function Modal({open,onClose,title,subtitle,children,size=''}){if(!open)return null;return <div className="modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><section className={`modal ${size}`}><header><div><h2>{title}</h2>{subtitle&&<p>{subtitle}</p>}</div><button className="icon-button" onClick={onClose}><X/></button></header><div className="modal-body">{children}</div></section></div>}
export function EmptyState({title='Nothing here yet',message='Create your first record to get started.',action}){return <div className="empty-state"><span><Inbox/></span><h3>{title}</h3><p>{message}</p>{action}</div>}
export function Loading(){return <div className="loading"><LoaderCircle/> Loading your workspace…</div>}
export function ErrorState({title='Unable to load this page',message="We couldn't reach the server. Your data has not been changed.",retry}){return <div className="error-state"><b>{title}</b><p>{message}</p>{retry&&<button className="secondary" onClick={retry}>Retry</button>}</div>}
export function Pagination({meta,onPage}){if(!meta||meta.pages<=1)return null;return <div className="pagination"><span>Showing page <b>{meta.page}</b> of {meta.pages} · {meta.total} results</span><div><button disabled={meta.page<=1} onClick={()=>onPage(meta.page-1)}><ChevronLeft/></button><button disabled={meta.page>=meta.pages} onClick={()=>onPage(meta.page+1)}><ChevronRight/></button></div></div>}
export function Field({label,error,children,hint}){return <label className={`field ${error?'has-error':''}`}><span>{label}</span>{children}{hint&&<small>{hint}</small>}{error&&<small>{error}</small>}</label>}
